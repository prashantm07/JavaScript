// Empty String
if ("") {
     // This block will NOT be executed
}

// Number '0'
if (0) {
     // This block will NOT be executed
}

// null
if (null) {
     // This block will NOT be executed
}

// undefined
if (undefined) {
     // This block will NOT be executed
}

// NaN (not a number)
if (NaN) {
     // This block will NOT be executed
}

// falys Expression
if (false && null && 0 && "") {
     // This block will NOT be executed
}
